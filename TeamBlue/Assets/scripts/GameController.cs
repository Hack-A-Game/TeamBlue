using UnityEngine;
using System.Collections;

public class GameController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//DeckBuilder.initDeck(deckPlayer);
		//DeckBuilder.initDeck(deckCPU);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
